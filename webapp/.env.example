# Copy this file to .env and fill in your values
# .env is gitignored — never commit it

# PicoVoice access key — from https://console.picovoice.ai/
# Note: this key will be visible in the compiled JS bundle (client-side SDK limitation)
VITE_PV_ACCESS_KEY=your-picovoice-access-key-here
